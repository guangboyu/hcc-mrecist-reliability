[project]
name = "hcc-mrecist-reliability"
version = "0.1.0"
description = "Reader reliability of mRECIST objective response after TACE, and its timepoint of origin"
requires-python = ">=3.11"
dependencies = [
  "numpy>=2.0",
  "openpyxl>=3.1",
  "pandas>=2.2",
  "scikit-learn>=1.6",
  "matplotlib>=3.9"
]

[tool.uv]
package = false
